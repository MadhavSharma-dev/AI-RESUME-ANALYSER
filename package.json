{
  "name": "ai-resume-analyser-root",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "22.x"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  }
}
